# یادداشت برای TAها

این مخزن برای یک بار استفاده ساخته نشده و در repository انجمن به‌صورت پابلیک می‌ماند تا دوره‌های بعدی هم از آن استفاده کنند.

## نقش TAها

- منابع مفید را از حالت `بررسی نشده` به `بررسی شده` یا `تأیید TA` منتقل کنید.
- اگر یک سؤال زیاد تکرار شد، آن را به یک منبع کوچک و قابل استفاده تبدیل کنید.
- اگر منبعی فقط به درد مطالعه عمومی می‌خورد، آن را حذف نکنید؛ فقط با برچسب `مطالعه اضافه` نگه دارید.
- رتبه‌بندی هر topic را در `ranking.md` همان topic به‌روز نگه دارید.

## حداقل معیار بررسی

1. آیا این منبع به یک نیاز واقعی جواب می‌دهد؟
2. آیا دانشجو می‌تواند سریع بفهمد الان به دردش می‌خورد یا نه؟
3. آیا متن از نظر فنی قابل دفاع است؟
4. اگر با AI ساخته شده، آیا ابزار، مدل و نوع استفاده شفاف ذکر شده است؟
