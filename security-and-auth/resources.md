# فهرست منابع Security و Authentication

فایل‌های واقعی این topic را داخل [files/](./files/) بگذارید. اگر منبع برای حل مستقیم این تمرین نیست، در توضیح آن برچسب `مطالعه اضافه` بزنید.

### SEC-101

- عنوان: Password hashing در Libsodium
- موضوع: password hashing
- سطح: متوسط
- زمان تقریبی مطالعه: ۱۲ دقیقه
- پیش‌نیازها: build پایه و کتابخانه خارجی
- چه زمانی باید این را بخوانیم؟ اگر می‌خواهید راه‌حل قابل دفاع‌تری از hash خام داشته باشید.
- خلاصه کوتاه: Libsodium برای password hashing از API سطح‌بالا و الگوریتم‌های مناسب مثل Argon2id استفاده می‌کند.
- مثال مرتبط با تمرین: اگر گروه شما می‌خواهد login را کمی جدی‌تر و قابل دفاع‌تر پیاده کند.
- لینک یا فایل منبع: [password-hashing-guide.md](./files/password-hashing-guide.md)
- نویسنده/اضافه‌کننده: curated
- وضعیت بررسی: بررسی شده

### SEC-102

- عنوان: The `pwhash*` API در Libsodium
- موضوع: password hashing API
- سطح: متوسط
- زمان تقریبی مطالعه: ۱۰ دقیقه
- پیش‌نیازها: C API پایه
- چه زمانی باید این را بخوانیم؟ وقتی می‌خواهید ببینید hash و verify در عمل چطور انجام می‌شود.
- خلاصه کوتاه: مثال‌های مستقیم برای ذخیره و verify کردن password دارد.
- مثال مرتبط با تمرین: جایگزین بهتر برای ذخیره plain password یا حتی SHA-256 خام.
- لینک یا فایل منبع: [password-hashing-guide.md](./files/password-hashing-guide.md)
- نویسنده/اضافه‌کننده: curated
- وضعیت بررسی: بررسی شده

### SEC-103

- عنوان: validation پایه برای register، login و post
- موضوع: input validation
- سطح: مقدماتی
- زمان تقریبی مطالعه: ۶ دقیقه
- پیش‌نیازها: ندارد
- چه زمانی باید این را بخوانیم؟ وقتی خطاهای ورودی زیاد شده‌اند.
- خلاصه کوتاه: خیلی از bugها با validation ساده حذف می‌شوند.
- مثال مرتبط با تمرین: نام کاربری تکراری، post خالی، like روی post ناموجود
- لینک یا فایل منبع: منبع داخلی همین topic
- نویسنده/اضافه‌کننده: curated
- وضعیت بررسی: بررسی شده
