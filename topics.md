# موضوعات این مخزن

## topicهای اصلی

### 1. C++

برای پیاده‌سازی کلاس‌ها، `STL`، فایل‌ها، مدیریت حافظه، `Rule of Three`، `smart pointer` و خطاها.

### 2. Git و GitHub

برای workflow گروهی، `clone`، `commit`، `branch`، `merge`، `pull request` و حل conflict.

### 3. ذخیره‌سازی داده

برای تصمیم بین فایل و `SQLite`، طراحی ساختار داده، serialization و بازیابی اطلاعات.

### 4. GUI

برای دانشجوهایی که می‌خواهند نسخه گرافیکی اختیاری بسازند یا از اول درباره انتخاب `Qt` یا `ImGui` تصمیم بگیرند.

### 5. Build System

برای `Make`، `CMake`، ساختار پوشه‌ها و ساخت پروژه روی سیستم‌های مختلف.

### 6. Backend و Frontend با C++

برای دانشجوهایی که می‌خواهند به‌جای app محلی صرف، یک backend با C++ بسازند و یک frontend وب یا جداگانه روی آن بگذارند.

## topicهای مکمل که از خود تمرین استخراج شدند

### 7. طراحی نرم‌افزار و معماری

چون تمرین روی مسئولیت کلاس‌ها، بزرگ‌نشدن `main` و توسعه‌پذیری حساس است.

### 8. Testing و Debugging

چون پروژه باید پایدار، بدون crash و قابل ارزیابی باشد.

### 9. Security و Authentication

چون در specification صریحاً گفته شده رمز عبور نباید به‌صورت plain text ذخیره شود.

### 10. اجرای همزمان و سلامت داده

چون فاز سوم روی چند اجرای همزمان ساده و سالم‌ماندن داده‌ها تأکید دارد.
