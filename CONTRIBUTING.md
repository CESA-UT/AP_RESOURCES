# راهنمای مشارکت

مشارکت در این مخزن باید ساده باشد. اگر چیزی بلد هستید که می‌تواند به بقیه برای حل `AP_1404_PRACTICE_ONE` کمک کند، همان را اضافه کنید؛ لازم نیست متن طولانی یا رسمی بنویسید.

## چطور یک منبع جدید اضافه کنیم؟

1. topic مناسب را پیدا کنید.
2. فایل `template.md` همان پوشه را کپی کنید.
3. اگر منبع فایل واقعی دارد، آن را داخل پوشه `files/` همان topic بگذارید.
4. محتوای منبع را کامل کنید.
5. یک ورودی کوتاه هم به `resources.md` همان topic اضافه کنید.
6. اگر لازم بود، در `ranking.md` همان topic هم آن را ثبت کنید.
7. commit بزنید و pull request باز کنید.

## فرمت پیشنهادی منبع

هر منبع بهتر است این بخش‌ها را داشته باشد:

- عنوان
- موضوع
- سطح
- زمان تقریبی مطالعه
- پیش‌نیازها
- چه زمانی باید این را بخوانیم؟
- خلاصه کوتاه
- مثال مرتبط با تمرین
- لینک یا فایل منبع
- نویسنده/اضافه‌کننده
- وضعیت بررسی
- ارتباط با تمرین: مستقیم / مرتبط ولی غیرالزامی / مطالعه اضافه
- استفاده از AI: خیر / بله
- اگر بله: ابزار، مدل و نوع استفاده

## قانون مهم برای دسترسی

- لینک اصلی که در `resources.md` ثبت می‌شود باید یا GitHubی باشد یا به فایل داخلی همین repository اشاره کند.
- اگر منبع اصلی روی سایتی بیرون از GitHub است، یک note، خلاصه، transcript یا نسخه `pdf/md` آن را داخل `files/` بگذارید و در `resources.md` به همان فایل داخلی لینک بدهید.

## چطور pull request بزنیم؟

1. مخزن را `clone` کنید.
2. یک `branch` جدید بسازید.
3. تغییرتان را انجام دهید.
4. commit معنادار بزنید.
5. branch را `push` کنید.
6. pull request باز کنید و توضیح بدهید این منبع چه مشکلی را حل می‌کند.

## چه چیزهایی قابل قبول نیستند؟

- منبعی که به تمرین ربط ندارد
- منبعی که به تمرین ربط مستقیم ندارد ولی به‌عنوان `مطالعه اضافه` هم برچسب نخورده
- متن خیلی کلی بدون کاربرد مشخص
- کپی‌کردن حجم زیادی از محتوا بدون ذکر منبع
- لینک بدون توضیح
- متن AI-generated که بازبینی نشده یا خطای فنی دارد

## استفاده مسئولانه از ChatGPT و ابزارهای مشابه

استفاده از AI ممنوع نیست. اما:

- خروجی باید توسط خودتان خوانده و اصلاح شده باشد.
- اگر مثال یا توضیح فنی داده‌اید، آن را روی مسئله واقعی تمرین منطبق کنید.
- اگر جایی شک دارید، وضعیت را `بررسی نشده` بگذارید.
- اگر متن را با کمک AI نوشته‌اید، در توضیح PR یا issue و خود منبع این قالب را شفاف بنویسید:

```text
استفاده از AI: بله
ابزار: ChatGPT Web
مدل: مثلا GPT-5
نوع استفاده: تولید پیش‌نویس / خلاصه‌سازی / بازنویسی / ترجمه / تولید مثال
میزان بازبینی انسانی: کامل / جزئی
```

## قاعده ساده برای این مخزن

اگر یک دانشجو بتواند در کمتر از چند دقیقه بفهمد «این منبع الان به درد من می‌خورد یا نه»، منبع احتمالاً مناسب است.
