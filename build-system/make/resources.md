# منابع Make

### MAKE-101

- عنوان: A Simple Makefile
- موضوع: makefile basics
- سطح: مقدماتی
- زمان تقریبی مطالعه: ۸ دقیقه
- پیش‌نیازها: gcc/g++ پایه
- چه زمانی باید این را بخوانیم؟ وقتی می‌خواهید اولین `Makefile` را بنویسید.
- خلاصه کوتاه: یک مثال ساده و خوانا از dependencyها و targetها.
- مثال مرتبط با تمرین: build کردن `main.cpp` و چند کلاس جداگانه.
- لینک یا فایل منبع: [make-start-path.md](./files/make-start-path.md)
- نویسنده/اضافه‌کننده: curated
- وضعیت بررسی: بررسی شده
- ارتباط با تمرین: مستقیم
- استفاده از AI: خیر

### MAKE-102

- عنوان: Rule Syntax
- موضوع: syntax
- سطح: مقدماتی
- زمان تقریبی مطالعه: ۵ دقیقه
- پیش‌نیازها: ندارد
- چه زمانی باید این را بخوانیم؟ وقتی syntax Makefile خطا می‌دهد.
- خلاصه کوتاه: rule، prerequisite و recipe را جمع‌وجور و رسمی توضیح می‌دهد.
- مثال مرتبط با تمرین: فهم این‌که target `all` و `clean` دقیقاً چگونه تعریف می‌شوند.
- لینک یا فایل منبع: [make-start-path.md](./files/make-start-path.md)
- نویسنده/اضافه‌کننده: curated
- وضعیت بررسی: بررسی شده
- ارتباط با تمرین: مستقیم
- استفاده از AI: خیر

### MAKE-103

- عنوان: Phony Targets
- موضوع: clean/all
- سطح: مقدماتی
- زمان تقریبی مطالعه: ۵ دقیقه
- پیش‌نیازها: Makefile پایه
- چه زمانی باید این را بخوانیم؟ وقتی targetهای `clean` و `all` را می‌سازید.
- خلاصه کوتاه: یکی از رایج‌ترین جزئیات مفید Makefile همین `.PHONY` است.
- مثال مرتبط با تمرین: `make clean` و `make run`.
- لینک یا فایل منبع: [make-start-path.md](./files/make-start-path.md)
- نویسنده/اضافه‌کننده: curated
- وضعیت بررسی: بررسی شده
- ارتباط با تمرین: مستقیم
- استفاده از AI: خیر
