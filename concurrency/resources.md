# فهرست منابع اجرای همزمان

فایل‌های واقعی این topic را داخل [files/](./files/) بگذارید. اگر منبع برای حل مستقیم این تمرین نیست، در توضیح آن برچسب `مطالعه اضافه` بزنید.

### CON-001

- عنوان: note داخلی: requirement اجرای همزمان در این تمرین دقیقاً یعنی چه؟
- موضوع: problem framing
- سطح: مقدماتی
- زمان تقریبی مطالعه: ۷ دقیقه
- پیش‌نیازها: ذخیره‌سازی پایه
- چه زمانی باید این را بخوانیم؟ قبل از over-engineering
- خلاصه کوتاه: این تمرین real-time networking نمی‌خواهد؛ فقط می‌خواهد دو اجرای ساده با refresh دوباره بتوانند داده سالم ببینند.
- مثال مرتبط با تمرین: کاربر اول post می‌گذارد، کاربر دوم با reload آن را می‌بیند.
- لینک یا فایل منبع: [concurrent-save-note.md](./files/concurrent-save-note.md)
- نویسنده/اضافه‌کننده: curated
- وضعیت بررسی: بررسی شده

### CON-002

- عنوان: SQLite locking و concurrency
- موضوع: file consistency
- سطح: متوسط
- زمان تقریبی مطالعه: ۱۰ دقیقه
- پیش‌نیازها: فایل و persistence
- چه زمانی باید این را بخوانیم؟ وقتی saveهای همزمان می‌تواند داده را خراب کند.
- خلاصه کوتاه: اگر مسیر SQLite را انتخاب کرده‌اید، این سند رسمی برای فهم locking و concurrency مرجع اصلی است.
- مثال مرتبط با تمرین: دو اجرای ساده برنامه که به یک فایل database مشترک وصل‌اند.
- لینک یا فایل منبع: [sqlite-concurrency-note.md](./files/sqlite-concurrency-note.md)
- نویسنده/اضافه‌کننده: curated
- وضعیت بررسی: بررسی شده
